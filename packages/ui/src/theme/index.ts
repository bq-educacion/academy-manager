import baseStyles from "./base-styles";
import breakpoints from "./breakpoints";
import colors, { adjustColorOpacity } from "./colors";
import fonts from "./fonts";
import { P1, P2, P3, P4, PG, A, RainbowDivider } from "../theme/styles";

export { adjustColorOpacity, baseStyles, breakpoints, colors, fonts, P1, P2, P3, P4, PG, A, RainbowDivider };
