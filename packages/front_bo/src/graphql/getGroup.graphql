query GetGroup($getGroupId: String!) {
  getGroup(id: $getGroupId) {
    group {
      course {
        EPO
        ESO
      }
      center {
        name
      }
      name
      modality
      type
      instructors {
        name
      }
      notes
      createdAt
      timetable {
        id_day
        day
        start
        end
      }
    }
    totalStudents
  }
}
