query GetCentersF(
  $centers: GetCentersInput!
) {
  getCenters(
    centers: $centers
  ) {
    page
    pageSize
    totalPages
    totalNumber
    data {
      id
      name
      languages
      city
      nature
      type
      active
    }
  }
}
